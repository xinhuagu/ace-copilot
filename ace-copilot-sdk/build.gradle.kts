// ace-copilot-sdk: API contracts for tools, plugins, and extensions
// No dependencies on other ace-copilot modules — this is the extension API surface

dependencies {
    // SDK depends only on Jackson annotations for serialization contracts
    implementation("com.fasterxml.jackson.core:jackson-annotations")
}
