/**
 * AceCopilot Memory — context management, auto-memory, self-learning, HMAC-signed memory files.
 *
 * <p>Three-tier memory model: short-term (conversation), medium-term (session summaries),
 * long-term (project + auto-memory with HMAC-SHA256 signing).
 */
package dev.acecopilot.memory;
