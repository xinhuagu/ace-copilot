/**
 * AceCopilot Daemon — persistent system process with boot, lock, session management, and UDS listener.
 *
 * <p>Turns the device into an always-on AI agent. Manages lifecycle,
 * serves multiple clients, and enables proactive capabilities.
 */
package dev.acecopilot.daemon;
