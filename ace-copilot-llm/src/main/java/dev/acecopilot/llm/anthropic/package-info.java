/**
 * Anthropic Claude API client implementation.
 *
 * <p>Uses {@link java.net.http.HttpClient} for HTTP communication and
 * Server-Sent Events (SSE) for streaming responses on virtual threads.
 */
package dev.acecopilot.llm.anthropic;
