/**
 * AceCopilot Core — agent loop, task planner, tool system, LLM client abstractions, agent teams.
 *
 * <p>The heart of the system. Contains the ReAct agent cycle, tool registry,
 * message protocol, and multi-agent orchestration.
 */
package dev.acecopilot.core;
