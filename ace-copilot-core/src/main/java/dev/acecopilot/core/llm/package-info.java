/**
 * LLM client abstractions — provider-agnostic interfaces for language model interaction.
 *
 * <p>Defines the contract that all LLM provider implementations must fulfill,
 * including request/response models, streaming, and tool-use support.
 */
package dev.acecopilot.core.llm;
